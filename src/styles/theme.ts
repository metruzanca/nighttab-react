export const theme = {
  borderRadius: ".25em",
  linkItemWidth: "11em",
  linkItemHeight: "10em",
  rootFontSize: "16px",
}

// function createSheet(data){
//   return Object.entries(data)
//     .map(([k,v]) => `--${k}: ${v};\n`)
//     .join("")
// }

// function cssVar(key: keyof typeof flat){
//   return `var(--${key})`
// }